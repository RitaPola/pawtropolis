animal(id,favorite_food, year, admission_date,weight,height)

lion(id_animal*, tail_length)

tiger(id_animal*, tail_length)

eagle(id_animal*, longest_tails)

item(id, name, description, occupied_slots, bag_id*)

bag(id, total_slots)

player(id, name,health, id_bag*)

room(id, name, id_animal*, id_items*)

direction(id, name)

room_direction(id,room1_id,room2_id,direction_id*)

